const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),{JSDOM}=require('jsdom');
const root=path.join(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('every P1 entry includes shared reading speed; five choices persist and update new song players',async()=>{for(const page of ['index.html','lesson.html','literature/index.html','00-prerequisite/index.html','workbook/index.html']){const d=new JSDOM(read(page),{url:'https://webbase-x.github.io/is/P1/'+page,runScripts:'outside-only'}),w=d.window;assert.ok([...w.document.scripts].some(s=>s.src.includes('reading-speed.js')));w.localStorage.setItem('p1-reading-speed','1.2');w.eval(read('reading-speed.js'));await new Promise(r=>setTimeout(r,5));const s=w.document.querySelector('#speed,#p1ReadingSpeed');assert.deepEqual([...s.options].map(o=>o.text),['ช้า','ช้าพอดี','ปกติ','เร็ว','เร็วมาก']);assert.equal(w.P1ReadingSpeed.get(),1.2);let changed=0;w.document.addEventListener('p1-reading-speed-change',()=>changed++);s.value='1.45';s.dispatchEvent(new w.Event('change'));assert.equal(changed,1);assert.equal(w.localStorage.getItem('p1-reading-speed'),'1.45');const a=w.document.createElement('audio');w.document.body.append(a);await new Promise(r=>setTimeout(r,5));assert.equal(a.playbackRate,1.45);assert.equal(a.preservesPitch,true);s.value='0.65';s.dispatchEvent(new w.Event('change'));assert.equal(a.playbackRate,.65);d.window.close()}});
test('literature voice uses selected speed and changing it cancels active highlighting',async()=>{const d=new JSDOM(read('literature/index.html'),{url:'https://webbase-x.github.io/is/P1/literature/?chapter=1&page=4',runScripts:'outside-only'}),w=d.window,spoken=[];w.fetch=async()=>({ok:true,json:async()=>JSON.parse(read('literature/book.json'))});w.scrollTo=()=>{};w.FontFace=class{async load(){return this}};w.document.fonts={add(){}};w.Image=class{async decode(){}};w.SpeechSynthesisUtterance=function(t){this.text=t};w.speechSynthesis={cancel(){},getVoices:()=>[],speak:u=>spoken.push(u)};w.eval(read('reading-speed.js'));w.eval(read('course-flow.js'));w.eval(read('literature/reader.js'));await new Promise(r=>setTimeout(r,20));const s=w.document.querySelector('#speed');s.value='1.45';s.dispatchEvent(new w.Event('change'));w.document.querySelector('.word').dispatchEvent(new w.MouseEvent('click',{bubbles:true}));assert.equal(spoken[0].rate,1.45);assert.equal(w.document.querySelectorAll('.speaking').length,1);s.value='.65';s.value='0.65';s.dispatchEvent(new w.Event('change'));assert.equal(w.document.querySelectorAll('.speaking').length,0);d.window.close()});
